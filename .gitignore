# --- Go build output ---
/rv
/dist/
/bin/
*.exe
*.test
*.out

# Coverage
coverage.out
coverage.html

# Go workspace files (per-developer, never committed)
go.work
go.work.sum

# --- Editors / OS ---
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# --- Local environment ---
.env
.envrc

# --- Agent data ---
.antigravitycli/

# --- Revive runtime artifacts ---
# Generated by a restore; never source of truth.
manifest.lock
manifest-*.lock

# --- reference/ : the archived Python implementation ---
# Kept as a behavioral oracle, not built or run in CI.
reference/**/__pycache__/
reference/**/*.py[cod]
reference/**/*.egg-info/
reference/.venv/
reference/.mypy_cache/
reference/.pytest_cache/
reference/.ruff_cache/
reference/.coverage
reference/build/
reference/dist/
