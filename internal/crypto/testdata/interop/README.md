# Compatibility fixtures

These committed fixtures were produced by an earlier release and prove that its ciphertext stays
decryptable without retaining or running that release's source tree.

The keys are disposable test keys and protect nothing.
